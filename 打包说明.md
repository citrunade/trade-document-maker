# 外贸制单工具 —— 打包与运行说明

## 一、环境准备

1. 安装 Python 3.10 及以上版本（Windows 10/11 64位）。
2. 在项目根目录（本文件所在目录）打开命令行，安装依赖：

   ```
   pip install -r requirements.txt
   ```

## 二、开发环境运行

```
python main.py
```

首次运行会自动在程序根目录创建 `data/` 文件夹，并写入默认的公司信息、
1 条示例客户、5 条示例物料数据、以及 Own/Delivery/Conditions/Banking 四类模板各 1 套默认预设，
方便直接体验制单流程。

### 单据模型说明（重要变更）

从本版本起，制单不再一次性同时生成 PI/CI/PL 三份单据，而是**一次生成一份单据**，
通过「单据类型」下拉框选择 PI（形式发票）/ CI（商业发票）/ PL（装箱单），
三者格式完全统一，仅标题与是否显示价格不同（PL 不显示单价/金额）。

单据编号规则：`BAAS` + 当天日期(YYMMDD) + 随机字母(A-H)，例如 `BAAS260726A`。

制单时需从「模板管理」页签维护的四类预设中各选择一套应用到当前单据：
- **Own**：本公司信头（Logo/名称/地址/电话/公司注册号/GST号），显示在单据**右上角**（含 Logo，随单据类型固定不变）
- **Delivery**：收货地址，与收件方信息配对显示在页面**左侧**
- **Conditions**：付款方式/贸易术语/运输方式，显示在收货地址下方（左侧）
- **Banking**：银行基本信息（每页页脚显示）+ 收款银行详情（仅末页显示，右侧信息栏关联）

收件方（客户档案中选择的客户，随单据类型不同可能是买方或卖方）显示在单据**左上角**。
Own 与收件方信息会重复显示在每一页页眉，银行基本信息重复显示在每一页页脚。

### 导出格式

制单页面与单据历史页面均可在导出时选择格式：**PDF / Word (.docx) / Excel (.xlsx)**。
三种格式内容一致（同一份单据的所有字段），仅呈现方式不同：
- PDF：固定排版页面，Own/收件方信息与银行信息通过页眉页脚在每页重复显示。
- Word：使用 Word 原生页眉页脚机制，同样在每页自动重复显示 Own/收件方/银行信息。
- Excel：结构化电子表格，适合需要进一步筛选/复制数据的场景。

## 三、打包为单文件 EXE

在项目根目录运行：

```
pyinstaller build.spec --noconfirm
```

打包完成后，EXE 文件位于：

```
dist\外贸制单工具.exe
```

只需将这一个 EXE 文件复制到任意 Windows 10/11 电脑上即可直接双击运行，
无需安装 Python 或任何依赖库。

## 四、重要说明：data 目录与 EXE 同级

- 软件所有数据（公司信息、客户、物料、单据历史、Logo、备份、导出的 PDF）
  均保存在 **EXE 文件所在目录** 下的 `data/` 文件夹中，而不是 Windows 临时目录。
- 这意味着：
  - 卸载/删除软件只需删除 EXE 和 `data/` 文件夹即可，不会在系统其他地方残留文件。
  - 如果要迁移到另一台电脑，只需把 EXE 和 `data/` 文件夹一起复制过去。
  - **不要**把 EXE 放在只读目录（如某些云盘同步目录、Program Files 等系统受保护目录），
    否则可能因权限问题无法创建 `data/` 文件夹。建议放在如 `D:\外贸制单工具\` 这样的
    普通目录下运行。

## 五、中文字体说明（PDF 导出）

- PDF 导出会自动优先使用 Windows 系统自带的「微软雅黑」(`C:\Windows\Fonts\msyh.ttc`)。
- 如果系统没有微软雅黑，会自动回退使用「宋体」(`simsun.ttc`)。
- 如果以上两种字体都无法读取（例如字体文件被删除等极端情况），程序会自动使用
  ReportLab 内置的中文字体作为最终保底方案，**不会因缺字体而崩溃或乱码**。
- 因此打包时无需额外携带字体文件，也无需在 PyInstaller 中做任何字体相关的
  `--add-data` 配置。

## 六、Logo 说明

- 在软件的「设置」页签中点击「选择/替换 Logo 图片」上传 PNG/JPG 图片，
  会自动保存为 `data/logo.png`。
- 生成 PDF 时会自动读取并等比例缩放显示在单据页眉；如果尚未上传 Logo，
  PDF 会自动跳过图片、仅显示公司名称文字，不会报错或出现空白图框。

## 七、AI 导入功能说明（阿里云百炼 / Qwen）⚠️ 唯一联网功能

软件新增了「AI 导入」功能，可从 PDF / Excel / Word(.docx) / JPG / PNG 文件中自动识别并提取字段
（客户信息、产品信息、采购订单明细），减少手动重复录入。

**这是本软件唯一需要联网的功能**，其余所有功能（制单、PDF 导出、数据存储等）
仍然完全本地运行、不联网。请知悉：

- 使用该功能时，所选文件的内容（文字或图片）会被发送至阿里云百炼（Qwen）云端 API 进行识别。
- 首次点击任意「AI 导入」按钮时，软件会弹窗提示并需要用户确认后才会继续。
- 该功能默认关闭（不影响其他功能使用），只有在「设置」页签填写阿里云百炼 API Key 后才能使用。

**获取 API Key 步骤：**

1. 前往阿里云百炼（Model Studio）开通服务并创建中国北京地域的 API Key：
   <https://bailian.console.aliyun.com/>
2. 软件默认使用 `qwen-flash`（文本模型）与 `qwen3-vl-flash`（视觉模型）。
   如需更换模型，可在「设置」页签中填写其他兼容模型 ID。
3. 默认 API 地址为中国北京地域：
   `https://dashscope.aliyuncs.com/compatible-mode/v1`。
   API Key 与调用地域必须一致，否则会返回 401。
4. 将 API Key 填入本软件「设置」页签的「AI 文件导入设置」区域，
   点击「测试连接」确认可用。

**费用说明：** 百炼按实际 Token 使用量计费，Qwen Flash / VL Flash 属于低成本模型。
价格、免费试用额度与模型可用性可能调整，请以阿里云百炼当前定价为准。

**使用位置：**
- 客户档案页签 →「AI 导入客户」：从客户资料/名片等文件中提取客户信息。
- 物料库页签 →「AI 批量导入」：从产品规格表/报价单中批量提取产品信息。
- 制单页签 →「AI 导入产品明细」：从客户采购订单 (PO) 中提取产品明细行。

以上三处均会先弹出「审核」表格，AI 识别的内容不会直接保存，
用户可勾选/取消勾选每一行，并可直接修改表格中的文字后再确认导入。

**文字优先、图片兜底策略：** 为控制成本并兼顾识别效果，各文件类型均遵循「优先取文字、必要时才用图片」的原则：

- **PDF**：若页面本身包含可提取的文字层（大多数电子生成的单据），直接将文字发送给文本模型；
  若某页没有文字层（扫描件/拍照件），才会将该页渲染为图片交给视觉模型识别。
- **Excel**：优先读取单元格文字内容；若表格文字内容过于稀疏（例如产品信息是以图片形式贴在
  单元格中，而非文字），会自动改用表格中嵌入的图片交给视觉模型识别。
- **Word (.docx)**：优先提取段落与表格文字；若文字内容过于稀疏（例如整份文档以图片排版），
  会自动改用文档中嵌入的图片交给视觉模型识别。仅支持 `.docx` 格式，旧版 `.doc` 请先在 Word
  中另存为 `.docx` 后再导入。
- **JPG/PNG**：直接调用视觉模型识别。

## 八、常见打包问题排查

| 问题 | 原因 | 解决方法 |
|---|---|---|
| 双击 EXE 一闪而退 | 通常是权限问题导致无法创建 `data/` 目录 | 将 EXE 移动到非系统保护目录（如 D 盘任意文件夹）后重试 |
| PDF 打开中文显示乱码/方块 | 极少数精简版 Windows 系统缺少中文字体 | 确认 `C:\Windows\Fonts` 下存在 `msyh.ttc` 或 `simsun.ttc`；若都没有，程序会自动使用内置回退字体（无需处理） |
| 打包体积较大（约 60-70MB） | PyQt6 + pandas + reportlab 均为较大的第三方库 | 属正常现象，可忽略；如需精简体积可自行研究 `--exclude-module` 排除未用到的 pandas 子模块，但需谨慎测试 Excel 导入功能是否仍正常 |
| 杀毒软件报毒/拦截 | PyInstaller 打包的 EXE 有时会被部分杀毒软件误报 | 属已知的 PyInstaller 通病，可将 EXE 添加到杀毒软件信任列表/白名单 |
| 「AI 导入」提示"尚未设置 API Key" | 未在设置页签填写百炼 API Key | 前往「设置」页签填写 API Key 并点击「测试连接」确认可用 |
| 「AI 导入」提示"无法连接阿里云百炼 API" | 网络、代理或 VPN 异常，或无法访问 DashScope | 检查网络、Windows 代理/VPN，并确认可访问 `dashscope.aliyuncs.com` |
| 「AI 导入」提示"AI 返回的内容不是有效的 JSON" | 文件内容模糊/排版特殊导致模型未能按要求格式返回 | 尝试重新导入，或改用图片更清晰的扫描件；也可在审核表格中手动补全字段 |

## 九、项目结构说明

```
invoice maker/
├── main.py                # 程序入口
├── build.spec             # PyInstaller 打包配置
├── requirements.txt       # Python 依赖列表
├── core/                  # 核心业务逻辑（无 UI 依赖，可独立测试）
│   ├── paths.py           # 路径管理（区分开发环境/打包后 EXE）
│   ├── storage.py         # JSON 本地存储引擎、备份还原
│   ├── models.py          # 数据模型（客户/物料/单据/明细行）
│   ├── calc.py            # 金额/重量/体积计算、大写金额转换
│   ├── fonts.py           # PDF 中文字体注册（含多级回退）
│   ├── pdf_export.py      # 单据 PDF 生成
│   ├── word_export.py     # 单据 Word (.docx) 生成
│   ├── excel_export.py    # 单据 Excel (.xlsx) 生成
│   ├── document_export.py # 导出格式分发器（PDF/Word/Excel 统一入口）
│   ├── seed.py            # 内置默认示例数据（含四类模板预设）
│   ├── ai_client.py       # 阿里云百炼/Qwen API 调用封装（文本/视觉）
│   └── ai_import.py       # AI 文件导入：PDF/Excel/Word/图片 -> 结构化字段
├── ui/                    # PyQt6 界面
│   ├── main_window.py     # 主窗口，整合各 Tab 页
│   ├── settings_tab.py    # 设置（Logo/备份还原/百炼 API Key）
│   ├── customers_tab.py   # 客户档案管理（含 AI 导入客户）
│   ├── products_tab.py    # 物料库管理（含 Excel 批量导入、AI 批量导入）
│   ├── templates_tab.py   # 模板管理（Own/Delivery/Conditions/Banking 四类预设）
│   ├── document_tab.py    # 制单页面（联动计算 + PDF/Word/Excel 导出 + AI 导入明细）
│   ├── history_tab.py     # 单据历史（搜索/复制新建/重新导出）
│   └── ai_import_helper.py # AI 导入公共逻辑（隐私提示/文件选择/审核对话框）
├── docs/
│   └── 客户主体信息设计文档.md  # 客户字段规范、建表SQL参考、Excel表头
└── data/                  # 运行时自动生成，存放所有本地数据（不要手动删除或改名）
```
